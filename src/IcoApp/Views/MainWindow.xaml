<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="IcoApp.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IcoApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
   <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <TitleBar x:Name="AppTitleBar" Title="IcoApp" Subtitle="File.ico ●" Grid.ColumnSpan="2">
            <TitleBar.LeftHeader>
                <StackPanel Orientation="Horizontal" Margin="4,0,0,0">
                    <StackPanel.Resources>
                        <Style TargetType="Button" BasedOn="{StaticResource ControlButton}"/>
                        <Style TargetType="FontIcon">
                            <Setter Property="FontSize" Value="16"/>
                        </Style>
                    </StackPanel.Resources>

                    <Button>
                        <FontIcon Glyph="&#xE7C3;" />
                    </Button>
                    <Button>
                        <FontIcon Glyph="&#xE838;" />
                    </Button>
                    <Button>
                        <FontIcon Glyph="&#xE74E;" />
                    </Button>

                    <AppBarSeparator/>

                    <Button>
                        <FontIcon Glyph="&#xE713;" />
                    </Button>

                    <AppBarSeparator/>

                    <Button>
                        <FontIcon Glyph="&#xE7A7;" />
                    </Button>

                    <Button>
                        <FontIcon Glyph="&#xE7A6;" />
                    </Button>

                </StackPanel>
            </TitleBar.LeftHeader>
        </TitleBar>

        <Border
            Grid.Row="1"
            Margin="8,0,8,8"
            MinWidth="160" 
            Background="{ThemeResource ControlFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
            BorderThickness="1" CornerRadius="4">

            <StackPanel>
                <StackPanel HorizontalAlignment="Center" Orientation="Horizontal" Margin="8">
                    <StackPanel.Resources>
                        <Style TargetType="FontIcon">
                            <Setter Property="FontSize" Value="16"/>
                        </Style>
                    </StackPanel.Resources>
                    
                    <Button Style="{ThemeResource AccentButtonStyle}" Margin="4">
                        <FontIcon Glyph="&#xE710;" />
                    </Button>
                    
                    <Button Margin="4">
                        <FontIcon Glyph="&#xE74D;" />
                    </Button>
                </StackPanel>
            </StackPanel>
            
        </Border>
    </Grid>
</Window>